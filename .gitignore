# Terraform
.terraform/
.terraform.lock.hcl
tfplan
*.tfstate
*.tfstate.backup

# Env-specific tfvars are committed (they contain no secrets)
# Secrets are passed via TF_VAR_* env vars in CI

# Local secret files
secrets.*.env

# OS
.DS_Store
